import React from 'react';
import Aux from '../hoc/Aux';
import NavBar from '../Nav/NavBar'

const layout = (props) => (
    <Aux>
        <main>
            <NavBar></NavBar>
            
        </main>
    </Aux>
);

export default layout;
